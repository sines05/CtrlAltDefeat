---
name: media-manifest-first
description: CtrlAltDefeat asset integration should ship as a backend-approved media manifest and API for current models plus 10 process videos, without adding a public gallery, RAG expansion, or upload system.
metadata:
  type: project
---
Asset integration scope is manifest+API first: current FBX/GLB models and 10 MP4 process videos should be exposed as backend-approved media metadata, while the approved five-step tour stays unchanged.

**Why:** The user explicitly chose model+video integration first, rejected a public JPEG gallery/RAG expansion for now, and does not want an upload system in this phase.

**How to apply:** Prefer a thin approved JSON manifest plus read-only API over new ingestion tooling; keep process-video stations separate from the editorial five-step tour; avoid recommendations that require gallery search, public asset browsing, or generalized CMS features.
