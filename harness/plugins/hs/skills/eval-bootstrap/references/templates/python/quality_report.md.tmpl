# Quality Assessment Report — {domain}

**Date**: {date}
**Judge Model**: {model}
**Cases Evaluated**: {n_cases}
**Deterministic Verdict**: {deterministic_verdict}
**Judge Advisory Score**: {overall_score}/100

---

## 1. Summary

The deterministic gate returned **{deterministic_verdict}** (maturity
{deterministic_maturity}%, threshold {threshold}%). The LLM judge separately
scored holistic quality at **{overall_score}/100**. The judge score is
ADVISORY — it adds robustness, consistency, completeness, and precision review
on top of the deterministic field-matching, but it does not change the verdict.

| Metric | Value |
|--------|-------|
| Deterministic match rate | {deterministic_rate}% |
| Deterministic maturity | {deterministic_maturity}% |
| Deterministic P0 gate | {p0_gate_status} |
| **Deterministic verdict (authoritative)** | **{deterministic_verdict}** |
| Judge advisory score | {overall_score}/100 |
| Judge advisory recommendation | {judge_recommendation} |

---

## 2. Dimension Breakdown (advisory)

| Dimension | Weight | Score | Weighted |
|-----------|:------:|:-----:|:--------:|
| Accuracy | {accuracy_weight}% | {accuracy_score} | {accuracy_weighted} |
| Robustness | {robustness_weight}% | {robustness_score} | {robustness_weighted} |
| Consistency | {consistency_weight}% | {consistency_score} | {consistency_weighted} |
| Completeness | {completeness_weight}% | {completeness_score} | {completeness_weighted} |
| Precision | {precision_weight}% | {precision_score} | {precision_weighted} |

{weakest_dimension_note}

---

## 3. Pattern Analysis

{patterns_section}

---

## 4. Improvement Recommendations

{recommendations_section}

---

## 5. Detailed Findings

{detailed_findings_section}

---

## 6. Raw Judge Output

<details>
<summary>Click to expand full judge JSON response</summary>

```json
{raw_judge_output}
```

</details>

---

*Advisory report generated by the eval-bootstrap quality judge on {date}. The
deterministic verdict above is the release gate; this report is advisory.*
