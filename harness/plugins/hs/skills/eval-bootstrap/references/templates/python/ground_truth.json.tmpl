{
  "description": "Ground truth annotations for ${domain}.",
  "items": [
    {
      "case_file": "example-1.${ext}",
      "ground_truth": {
        "field_name": "expected value",
        "optional_field": null
      },
      "notes": "Describe where the ground truth values come from in the source file."
    }
  ]
}
