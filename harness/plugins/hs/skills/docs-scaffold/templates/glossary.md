---
id: {{id}}
type: glossary
tier: platform
status: {{status}}
owner: {{owner}}
version: {{version}}
---

# {{title}} — Glossary

> Generated VIEW of `docs/glossary.yaml` (the single source of truth). Do NOT
> hand-edit the table below — a render overwrites it. Add a term with
> `glossary_register.py --root . --add` (or edit the YAML SSOT), then re-render.
> Shares the harness's four-field schema (`term` / `definition` / `forbidden` /
> `backing`) — one register, no forked schema.

## Term table

| Term | Definition | Forbidden wording | Backing |
|---|---|---|---|
| TBD | TBD — one-line definition | TBD — banned framing, or empty | TBD — DEC id / test / doc anchor |
