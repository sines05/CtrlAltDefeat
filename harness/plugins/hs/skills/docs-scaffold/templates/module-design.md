---
id: {{id}}
type: module-design
tier: L2
status: {{status}}
owner: {{owner}}
version: {{version}}
parent: {{parent}}
---

# {{title}} — Thiết kế chi tiết

> TBD — mục tiêu thiết kế module; phạm vi; ngoài-phạm-vi.

## 1. Bối cảnh & ràng buộc
> TBD — driver nghiệp vụ, NFR liên quan, ràng buộc (regulatory, legacy, SLA).

## 2. Cấu trúc trong (white-box)
> TBD — thành phần con / part, trách nhiệm từng phần, sơ đồ (link `_diagram/`).

## 3. Luồng xử lý chính
> TBD — happy path + nhánh lỗi; sự kiện vào/ra; idempotency.

## 4. Dữ liệu & trạng thái
> TBD — entity sở hữu, schema chính, vòng đời, lưu trữ/retention.

## 5. Hợp đồng tích hợp
> TBD — API/event in-out (link api.md), reuse part module khác (link `_index`).

## 6. Lỗi, biên & phục hồi
> TBD — failure mode, retry/fallback, degradation.

## 7. Quyết định thiết kế
> TBD — link ADR liên quan (`docs/decisions/adr/`), trade-off đã chốt.
