---
id: {{id}}
type: config
tier: L2
status: {{status}}
owner: {{owner}}
version: {{version}}
parent: {{parent}}
---

# {{title}} — Recipe / cấu hình

<!-- Loại config thuộc PTSP sở hữu: PTSP điền & duy trì recipe; engine PTNT chỉ đọc. -->

> TBD — cấu hình này điều chỉnh hành vi gì; áp cho tenant/scope nào.

## 1. Tham số
> TBD — danh sách tham số + ý nghĩa + mặc định.

## 2. Schema config
> TBD — schema chính thức (kiểu, bắt buộc/optional); link schema file nếu có.

## 3. Ví dụ
> TBD — ví dụ config tối thiểu + đầy đủ.

## 4. Ràng buộc / validation
> TBD — quy tắc hợp lệ, phụ thuộc giữa tham số, giới hạn.

## 5. Vòng đời thay đổi
> TBD — quy trình đổi config (review, rollout, rollback).
