---
id: {{id}}
type: techstack
tier: platform
status: {{status}}
owner: {{owner}}
version: {{version}}
---

# {{title}} — Tech stack

> TBD — phạm vi stack được mô tả (toàn nền tảng / một mảng).

## 1. Ngôn ngữ / runtime
> TBD — ngôn ngữ + phiên bản runtime sử dụng.

## 2. Framework chính
> TBD — framework cốt lõi cho từng tầng (api, worker, frontend...).

## 3. Hạ tầng / data store
> TBD — DB, cache, queue, object store + vai trò.

## 4. Tích hợp ngoài
> TBD — dịch vụ/bên thứ ba phụ thuộc.

## 5. Ràng buộc phiên bản
> TBD — phiên bản tối thiểu/khoá, ma trận tương thích.

## 6. Lý do chọn
> TBD — vì sao chọn các thành phần trên; link ADR liên quan.
