---
id: {{id}}
type: agent-spec
tier: L3
status: {{status}}
owner: {{owner}}
version: {{version}}
parent: {{parent}}
---

# {{title}} — Đặc tả AI agent

> TBD — agent này làm gì, thay con người ở đâu.

## 1. Mục đích & ranh giới
> TBD — mục đích chính + điều agent KHÔNG được làm.

## 2. Năng lực / Tool
> TBD — danh sách tool agent được dùng; link mcp-tool.md tương ứng.

## 3. I/O contract
> TBD — input agent nhận, output agent trả (định dạng, schema).

## 4. Guardrail & quyền
> TBD — chính sách an toàn, giới hạn quyền, dữ liệu được phép truy cập.

## 5. Fallback ladder
> TBD — thứ tự xử lý khi thất bại (retry → degrade → human handoff).

## 6. Eval
> TBD — cách đánh giá agent; link eval.md.

## 7. Model
> TBD — model nền dùng; link model-card.md.
