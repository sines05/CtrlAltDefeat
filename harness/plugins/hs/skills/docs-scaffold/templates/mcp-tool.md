---
id: {{id}}
type: mcp-tool
tier: L3
status: {{status}}
owner: {{owner}}
version: {{version}}
parent: {{parent}}
---

# {{title}} — MCP / Tool

> TBD — tool này cho agent làm được gì.

## 1. Tên & mục đích
> TBD — tên tool (định danh gọi) + mục đích.

## 2. Input schema
> TBD — schema tham số đầu vào (kiểu, bắt buộc/optional).

## 3. Output schema
> TBD — schema kết quả trả về.

## 4. Side-effect & quyền
> TBD — tác dụng phụ (ghi/xoá/gọi ngoài) + quyền cần.

## 5. Lỗi
> TBD — các lỗi có thể xảy ra + ý nghĩa.

## 6. Ví dụ gọi
> TBD — ví dụ một lần gọi input → output.
