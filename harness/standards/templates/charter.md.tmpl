---
id: CHARTER
type: charter
status: {{status}}
owner: {{owner}}
version: {{version}}
# The Architecture Charter lists the goals every standard area must address.
# Each goal carries a REQUIRED metrics: list — a goal without a measurable
# target is not a charter goal. Goal ids follow ARCH-G<n>.
goals:
  - id: {{goal_id}}
    title: {{goal_title}}
    status: {{goal_status}}
    owner: {{owner}}
    metrics: {{goal_metrics}}
---

# Architecture Charter

Links the Engineering Vision (see vision.md) to the standard areas. Add one goal
entry per architectural objective; every standard area declares which goals it
addresses via its `arch_goals:` list.
