id: STACK
type: stack
status: {{status}}
owner: {{owner}}
version: {{version}}
# Pure-YAML SSOT: the techstack facts every standard area is written against —
# languages, runtimes, datastores, deployment targets, and the versions in
# force. Keep this current; standards downstream assume the stack named here.
description: |
  Languages: {{languages}}
  Datastores: {{datastores}}
  Deployment: {{deployment}}
